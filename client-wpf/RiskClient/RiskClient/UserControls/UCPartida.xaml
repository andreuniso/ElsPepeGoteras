<UserControl x:Class="RiskClient.UserControls.UCPartida"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="700"
             Height="70" Width="400" 
             x:Name="ucPartida">

    <Border CornerRadius="12" Background="#223B63" Padding="12" Margin="5" BorderBrush="#84b6f4" BorderThickness="1">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*" />
                <ColumnDefinition Width="1.5*" />
            </Grid.ColumnDefinitions>

            <!-- Nom de la partida -->
            <TextBlock Text="{Binding partida.Nom, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                       FontSize="26" FontWeight="Bold" Foreground="White"
                       VerticalAlignment="Center" HorizontalAlignment="Left" />

            <!-- Max jugadors -->
            <TextBlock Text="{Binding partida.MaxJugadors, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                       Grid.Column="1" FontSize="24" Foreground="White"
                       VerticalAlignment="Center" HorizontalAlignment="Center"/>
        </Grid>
    </Border>
</UserControl>
